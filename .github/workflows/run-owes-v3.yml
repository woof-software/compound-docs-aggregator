name: Run owes v3 calculation

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-owes-v3-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout org repository (code)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          lfs: false

      - name: Checkout artifacts repository (private)
        uses: actions/checkout@v4
        with:
          repository: cryptease/compound-docs-artifacts
          path: artifacts
          token: ${{ secrets.ARTIFACTS_TOKEN }}
          fetch-depth: 1
          lfs: false

      - name: Cache Git LFS objects (artifacts)
        uses: actions/cache@v4
        with:
          path: |
            artifacts/.git/lfs
            ~/.git/lfs
          key: ${{ runner.os }}-lfs-${{ github.ref_name }}-${{ hashFiles('artifacts/.gitattributes') }}
          restore-keys: |
            ${{ runner.os }}-lfs-${{ github.ref_name }}-
            ${{ runner.os }}-lfs-

      - name: Pull artifacts LFS (sqlite only)
        working-directory: artifacts
        run: |
          git lfs install --local --skip-smudge
          git config lfs.fetchinclude "storage/**/*.sqlite"
          git config lfs.fetchexclude ""
          git lfs pull

      - name: Overlay storage into workspace (required for owes)
        run: |
          mkdir -p artifacts/storage
          mkdir -p src/indexer/storage
          rsync -a artifacts/storage/ src/indexer/storage/

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Run owes v3 generation
        env:
          ANKR_KEY: ${{ secrets.ANKR_KEY }}
          UNICHAIN_QUICKNODE_KEY: ${{ secrets.UNICHAIN_QUICKNODE_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: yarn cli:generate:owes:v3

      # Push small snapshot to org repo
      - name: Commit & Push org snapshot (owes v3)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "github-actions"

          git add owes-v3.json
          git diff --cached --quiet || git commit -m "chore: run owes v3"
          git pull --rebase
          git push

      # Move detailed snapshot into artifacts repo and push it there
      - name: Sync detailed owes into artifacts repo
        run: |
          mkdir -p artifacts/snapshots
          cp -f owes-detailed-v3.json artifacts/snapshots/owes-detailed-v3.json

      - name: Commit & Push artifacts (detailed owes v3)
        working-directory: artifacts
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "github-actions"

          git add snapshots/owes-detailed-v3.json
          git diff --cached --quiet || git commit -m "chore: update detailed owes v3"
          git pull --rebase
          git push
